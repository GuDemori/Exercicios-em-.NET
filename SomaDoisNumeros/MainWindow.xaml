<Window x:Class="SomaDoisNumeros.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Soma de Dois Números" Height="200" Width="350">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0 5">
            <Label Content="Número 1:" Width="80"/>
            <TextBox x:Name="txtNumero1" Width="200"/>
        </StackPanel>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0 5">
            <Label Content="Número 2:" Width="80"/>
            <TextBox x:Name="txtNumero2" Width="200"/>
        </StackPanel>

        <Button Grid.Row="2" Content="Somar" Width="100" HorizontalAlignment="Right" Margin="0 5"
        Click="BtnSomar_Click"/>

        <Button Grid.Row="2" Content="Voltar ao Menu" Width="120" HorizontalAlignment="Left" Margin="0 5"
        Click="BtnVoltar_Click"/>
    </Grid>
</Window>